let originalGlaze = {glaze: 'Keep original', glazingPrice: 0}
let sugarGlaze = {'Sugar milk', glazingPrice: 0}
let vanillaGlaze = {glaze: 'Vanilla milk', glazingPrice: 0.5}
let chocolateGlaze = {glaze: 'Double chocolate', glazingPrice: 1.5}

// 
let basePrice = 2.49;
let something = document.getElementById("")

function priceFunction(){
    something.value = "Keep_Orig"
    final_price = (basePrice + glazingPrice) * packPrice
    something.innerHTML = final_price
}

for (let i = 0; i < glazing.length; i++) {
    // iterate through each 
}

// add to the UI
